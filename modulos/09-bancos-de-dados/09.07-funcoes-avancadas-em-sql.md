# Funções Avançadas em SQL

## Objetivo

Ao final desta aula, você deve ser capaz de:

- usar funções SQL para resumir dados;
- entender agregações comuns;
- agrupar registros com `GROUP BY`;
- filtrar grupos com `HAVING`.

## Onde Esta Aula Entra no Curso

- Módulo: 09. Bancos de Dados
- Aula anterior: Subqueries
- Próxima aula: Views
- Pré-requisitos: SQL básico, JOIN queries e subqueries.

## Ideia Central

Muitas perguntas de produto não pedem apenas registros individuais. Elas pedem contagens, somas, médias, maiores valores, menores valores e agrupamentos.

Funções avançadas em SQL permitem transformar várias linhas em respostas resumidas, como "quantas aulas cada usuário concluiu?" ou "qual tag aparece mais?".

## Tópicos da Aula

- `COUNT`.
- `SUM`.
- `AVG`.
- `MIN` e `MAX`.
- `GROUP BY`.
- `HAVING`.
- Funções de texto e data em nível introdutório.

## Exemplo Prático

```sql
SELECT usuario_id, COUNT(*) AS total_concluidas
FROM progresso
WHERE concluida = true
GROUP BY usuario_id
HAVING COUNT(*) >= 5;
```

Essa consulta conta aulas concluídas por usuário e mostra apenas quem concluiu pelo menos cinco.

## Analogias e Intuições

- Agregação é sair do detalhe e enxergar o resumo.
- `GROUP BY` cria pequenas pilhas de linhas antes de calcular.

## Erros Comuns

- Usar agregação sem entender o agrupamento.
- Confundir `WHERE` com `HAVING`.
- Selecionar colunas que não fazem parte do grupo.
- Esquecer que `COUNT(*)` conta linhas.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- O que `COUNT` calcula?
- Para que serve `GROUP BY`?
- Qual é a diferença entre `WHERE` e `HAVING`?
- Que pergunta de produto uma agregação responde?
- Por que agregações podem esconder detalhes importantes?

## Prática Recomendada

- Conte quantas tarefas existem por status.
- Calcule a média de duração estimada por projeto.
- Liste apenas grupos com mais de três itens.
- Explique cada parte da consulta em voz alta.

## Prompt Para Estudar Com IA

```text
Estou estudando funções avançadas em SQL em uma trilha de desenvolvimento fullstack JavaScript/TypeScript.

Meu objetivo é usar agregações, agrupamentos e filtros de grupo para responder perguntas de produto.

Contexto da aula:
- Descrição da aula: A aula apresenta COUNT, SUM, AVG, MIN, MAX, GROUP BY e HAVING.
- Descrição do módulo: O módulo ensina modelagem, SQL, consultas, integridade, performance, migrations, segurança, NoSQL e MongoDB.
- Itens já estudados: SQL básico, JOIN queries e subqueries.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, dê uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
