# Utility Types

## Objetivo

Ao final desta aula, você deve ser capaz de:

- explicar o que são utility types;
- usar `Partial`, `Pick`, `Omit` e `Record` em exemplos simples;
- reaproveitar tipos existentes sem duplicar formatos;
- entender a relacao entre utility types e generics;
- evitar tipos derivados que escondem intenção.

## Onde Esta Aula Entra na Formação

- Módulo: Programação com TypeScript
- Aula anterior: Generics
- Próxima aula: Tipos avançados
- Pré-requisitos: type aliases, interfaces, objetos e generics

## Ideia Central

Utility types são tipos prontos do TypeScript para transformar outros tipos. Eles ajudam a criar variações de um formato sem repetir tudo manualmente.

Isso é útil quando uma entidade aparece em contextos diferentes: criação, atualização, exibição, filtros e mapas.

## Tópicos da Aula

- `Partial<T>`
- `Pick<T, K>`
- `Omit<T, K>`
- `Record<K, T>`
- Tipos derivados
- Reaproveitamento sem duplicacao
- Limites de legibilidade

## Exemplo Prático

```typescript
type Usuario = {
  id: string;
  nome: string;
  email: string;
  ativo: boolean;
};

type AtualizacaoUsuario = Partial<Usuario>;
type UsuarioPublico = Omit<Usuario, "email">;
type ResumoUsuario = Pick<Usuario, "id" | "nome">;
type UsuariosPorId = Record<string, Usuario>;
```

## Analogias e Intuições

- Utility type é uma ferramenta para transformar formatos.
- `Pick` escolhe campos.
- `Omit` remove campos.
- `Partial` torna campos opcionais.

## Erros Comuns

- Usar `Partial` onde alguns campos deveriam continuar obrigatorios.
- Criar tipos derivados dificeis de ler.
- Usar utility type para evitar modelar um caso importante.
- Repetir tipos manualmente quando uma derivacao simples resolveria.
- Esquecer que tipos derivados não validam dados em runtime.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- O que é um utility type?
- Para que serve `Partial`?
- Qual a diferença entre `Pick` e `Omit`?
- Quando `Record` ajuda?
- Quando um tipo derivado pode prejudicar leitura?

## Prática Recomendada

- Crie um tipo `Produto`.
- Derive um tipo para criação sem `id`.
- Derive um tipo para atualização parcial.
- Crie um `Record` de produtos por código.

## Prompt Para Estudar Com IA

```text
Estou estudando utility types em TypeScript em uma trilha de desenvolvimento fullstack JavaScript/TypeScript.

Contexto da aula:
- Descrição da aula: Esta aula apresenta tipos utilitarios para transformar e reaproveitar formatos existentes.
- Descrição do módulo: O módulo Programação com TypeScript aprofunda JavaScript moderno e TypeScript como base de linguagem para o restante da formação.
- Itens já estudados: type aliases, interfaces, objetos e generics.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, de uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```

