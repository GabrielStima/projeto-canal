# Compatibilidade de Tipos

## Objetivo

Ao final desta aula, você deve ser capaz de:

- explicar a ideia de tipagem estrutural;
- entender por que dois objetos podem ser compativeis sem terem o mesmo nome;
- reconhecer propriedades obrigatorias e extras;
- prever atribuicoes validas em exemplos simples;
- preparar a base para interfaces, aliases e generics.

## Onde Esta Aula Entra na Formação

- Módulo: Programação com TypeScript
- Aula anterior: Anotações de tipos e tipos em funções
- Próxima aula: Narrowing
- Pré-requisitos: objetos, funções, anotações de tipos e inferência

## Ideia Central

TypeScript compara tipos principalmente pela estrutura. Se um valor tem as propriedades necessarias para ser usado em certo lugar, ele pode ser compativel, mesmo que tenha vindo de outro tipo nomeado.

Isso combina com a forma como JavaScript trabalha com objetos: o formato do valor importa muito.

## Tópicos da Aula

- Tipagem estrutural
- Propriedades obrigatorias
- Propriedades extras
- Compatibilidade em parâmetros
- Compatibilidade em atribuicoes
- Excesso de propriedades em objetos literais
- Cuidados com flexibilidade demais

## Exemplo Prático

```typescript
type UsuarioBasico = {
  nome: string;
};

const usuarioCompleto = {
  nome: "Ana",
  email: "ana@example.com",
};

function mostrarNome(usuario: UsuarioBasico) {
  console.log(usuario.nome);
}

mostrarNome(usuarioCompleto);
```

O objeto tem mais propriedades do que o necessário, mas possui a estrutura mínima esperada.

## Analogias e Intuições

- TypeScript pergunta: este valor tem o formato necessário?
- O nome do tipo ajuda humanos, mas a estrutura decide muita coisa.
- Flexibilidade e boa, desde que você saiba qual contrato importa.

## Erros Comuns

- Esperar que tipos sejam comparados apenas pelo nome.
- Achar estranho um objeto com propriedades extras ser aceito.
- Usar tipos amplos demais e perder proteção.
- Ignorar mensagens de excesso de propriedades em literais.
- Confundir compatibilidade estrutural com validação de dados externos.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- O que é tipagem estrutural?
- Por que um objeto com propriedade extra pode ser aceito?
- Quando um objeto não é compativel?
- O que uma função realmente exige de um parâmetro?
- Compatibilidade de tipos valida dados em runtime?

## Prática Recomendada

- Crie um tipo com uma propriedade obrigatória.
- Passe um objeto com propriedades extras para uma função.
- Remova uma propriedade obrigatória e observe o erro.
- Explique por escrito por que o TypeScript aceitou ou rejeitou cada caso.

## Prompt Para Estudar Com IA

```text
Estou estudando compatibilidade de tipos em uma trilha de desenvolvimento fullstack JavaScript/TypeScript.

Contexto da aula:
- Descrição da aula: Esta aula apresenta a tipagem estrutural do TypeScript e como objetos são comparados pelo formato.
- Descrição do módulo: O módulo Programação com TypeScript aprofunda JavaScript moderno e TypeScript como base de linguagem para o restante da formação.
- Itens já estudados: objetos, funções, inferência e anotações de tipos.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, de uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```

