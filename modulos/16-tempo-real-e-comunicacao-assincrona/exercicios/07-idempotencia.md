# Exercício 07 — Chaves de Idempotência

## Tarefa

Por que o método HTTP `POST` não é idempotente por natureza, enquanto o método `PUT` ou `DELETE` devem sê-lo? Explique o que o Desenvolvedor do PetCare precisa injetar (Ex: Chave de Idempotência no Body/Header) para curar o perigo das filas que rodam mensagens duplicadas (Retry/DLQ) quando fazem cobrança no Cartão.

## Corrija Sua Atividade Com IA

```text
Cenário: Curando duplicação de pacotes e Side-effects com Idempotência.

Tarefa: Minha explicação:
[COLE AQUI]

Critérios de correção:
1. A diferença clássica entre criar novos recursos (POST não-idempotente) vs manipular recursos baseados em IDs (PUT) foi demonstrada?
2. A importância vital da Chave Unica de Idempotência em integrações financeiras está clara?
```
