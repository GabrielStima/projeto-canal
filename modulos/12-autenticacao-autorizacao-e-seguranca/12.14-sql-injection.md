# SQL Injection

## Objetivo

Ao final desta aula, você deve ser capaz de:

- explicar SQL injection;
- reconhecer concatenação perigosa de query;
- usar parâmetros em vez de interpolar entrada;
- relacionar injection com validação e mínimo privilégio.

## Onde Esta Aula Entra na Formação

- Módulo: 12. Autenticação, Autorização e Segurança
- Aula anterior: XSS
- Próxima aula: Rate Limiting
- Pré-requisitos: SQL básico, queries, backend, APIs, validação, ORMs e segurança em bancos relacionais.

## Ideia Central

SQL injection acontece quando entrada controlada pelo usuário altera a estrutura de uma query. O atacante deixa de enviar apenas dado e passa a influenciar o comando executado pelo banco.

A defesa principal é usar queries parametrizadas ou APIs seguras do ORM. Validação ajuda, mas não substitui parâmetros. Permissões mínimas no banco reduzem impacto caso uma falha exista.

## Tópicos da Aula

- Entrada maliciosa.
- Concatenação de string.
- Query parametrizada.
- Prepared statement.
- ORM e query builder.
- Validação.
- Permissões do banco.
- Vazamento e alteração de dados.

## Exemplo Prático

```ts
// Perigoso
const sql = `SELECT * FROM users WHERE email = '${email}'`;

// Melhor ideia: parâmetros
const sql = "SELECT * FROM users WHERE email = ?";
const params = [email];
```

O objetivo é impedir que `email` mude a estrutura do comando SQL.

## Analogias e Intuições

- Parâmetro é colocar dado em um compartimento próprio, não misturar com o comando.
- Validar entrada é bom; parametrizar query é obrigatório.

## Erros Comuns

- Concatenar SQL com entrada do usuário.
- Achar que ORM torna qualquer uso automaticamente seguro.
- Validar apenas no frontend.
- Usar usuário do banco com permissão ampla demais.
- Exibir erro SQL completo para o cliente.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- Como dado vira comando em SQL injection?
- O que query parametrizada impede?
- Por que validação não basta?
- Como mínimo privilégio ajuda?
- Que erros não devem ser expostos?

## Prática Recomendada

- Escreva uma query vulnerável de busca por email.
- Reescreva usando parâmetros.
- Liste permissões mínimas para a API.
- Explique como um ORM pode ser usado de forma insegura.
- Defina resposta de erro segura para falha no banco.

## Prompt Para Estudar Com IA

```text
Estou estudando SQL injection em uma trilha fullstack JavaScript/TypeScript.

Meu objetivo é entender como entrada de usuário pode alterar queries e como evitar isso com parâmetros e mínimo privilégio.

Contexto da aula:
- Descrição da aula: A aula apresenta concatenação perigosa, query parametrizada, prepared statement, ORM, validação e permissões do banco.
- Descrição do módulo: O módulo ensina autenticação, autorização e segurança em aplicações web e APIs.
- Itens já estudados: SQL básico, bancos de dados, backend, APIs, validação e ORMs.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, dê uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
