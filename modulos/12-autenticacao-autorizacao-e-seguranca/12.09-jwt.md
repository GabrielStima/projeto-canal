# JWT

## Objetivo

Ao final desta aula, você deve ser capaz de:

- explicar o que é JSON Web Token;
- identificar header, payload e assinatura;
- entender claims, expiração e validação;
- reconhecer quando JWT ajuda e quando não é necessário.

## Onde Esta Aula Entra no Curso

- Módulo: 12. Autenticação, Autorização e Segurança
- Aula anterior: Autenticação via Token
- Próxima aula: Autorização
- Pré-requisitos: JSON, HTTP, autenticação via token, criptografia em alto nível, APIs e segurança na web.

## Ideia Central

JWT é um formato de token compacto composto por três partes: header, payload e assinatura. Ele permite que o servidor valide a integridade do token e leia claims sem consultar um storage central a cada requisição.

Essa conveniência tem trade-offs. JWT mal usado pode dificultar revogação, carregar dados demais, expor informações no payload e criar confusão entre autenticação e autorização.

## Tópicos da Aula

- Header.
- Payload.
- Assinatura.
- Claims.
- `exp`, `iat`, `iss`, `aud` e `sub`.
- Validação.
- Revogação.
- Dados sensíveis no payload.

## Exemplo Prático

```json
{
  "sub": "user_123",
  "iss": "api.estudos.local",
  "aud": "painel-estudos",
  "exp": 1780689600
}
```

O payload pode ser lido por quem tem o token. Por isso, ele não deve carregar senha, documento, segredo ou informação que dependa de confidencialidade.

## Analogias e Intuições

- JWT é um crachá assinado: dá para ler alguns dados, mas a assinatura mostra se alguém adulterou.
- Assinatura não significa criptografia do conteúdo.

## Erros Comuns

- Colocar dados sensíveis no payload.
- Não validar algoritmo, emissor, audiência e expiração.
- Usar JWT quando sessão simples resolveria melhor.
- Criar JWT sem plano de revogação.
- Confundir claim com permissão sempre atualizada.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- Quais são as três partes de um JWT?
- O payload é secreto?
- O que a assinatura garante?
- Quais claims precisam ser validadas?
- Por que revogar JWT pode ser mais difícil?

## Prática Recomendada

- Decodifique conceitualmente um JWT.
- Liste claims mínimas para uma API.
- Defina tempo de expiração.
- Explique como revogaria um token antes de expirar.
- Compare JWT com sessão server-side para o painel de estudos.

## Prompt Para Estudar Com IA

```text
Estou estudando JWT em uma trilha fullstack JavaScript/TypeScript.

Meu objetivo é entender o formato, as claims, a assinatura, a validação e os trade-offs de usar JWT.

Contexto da aula:
- Descrição da aula: A aula apresenta header, payload, assinatura, claims, expiração, validação, revogação e dados sensíveis.
- Descrição do módulo: O módulo ensina autenticação, autorização e segurança em aplicações web e APIs.
- Itens já estudados: JSON, HTTP, autenticação via token, cookies, sessões e APIs.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, dê uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
