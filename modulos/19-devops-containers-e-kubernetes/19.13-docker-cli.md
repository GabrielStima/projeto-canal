# Docker CLI

## Objetivo

Ao final desta aula, você deve ser capaz de:

- explicar o conceito principal da aula;
- reconhecer quando ele aparece em projetos reais;
- aplicar a ideia em um exemplo simples de entrega, container ou Kubernetes;
- saber o que estudar em seguida.

## Onde Esta Aula Entra na Formação

- Módulo: 19. DevOps, Containers e Kubernetes
- Aula anterior: Building Container Images
- Próxima aula: Running Containers
- Pré-requisitos: Git, GitHub, terminal, variáveis de ambiente e code review; Backend com Node.js, APIs, bancos de dados, segurança e observabilidade; Cloud Fundamentals e Infrastructure as Code; CI/CD, deploy e noções de artefatos de build.

## Ideia Central

A Docker CLI é a interface prática para construir, executar, listar, inspecionar, acompanhar logs e remover imagens e containers. O objetivo não é decorar comandos, mas entender quais perguntas cada comando responde sobre o estado do ambiente.

## Tópicos da Aula

- `docker build`
- `docker images`
- `docker run`
- `docker ps`
- `docker logs`
- `docker exec`
- `docker stop` e `docker rm`
- `docker inspect`

## Exemplo Prático

O Controle da Caixa (CLI):

O desenvolvedor júnior da clínica digita `docker run -d -p 8080:3000 petcare-api`.
O `-d` (detach) joga o serviço pro fundo e não trava o terminal.
O `-p 8080:3000` avisa: 'Se alguém bater na porta 8080 do meu notebook físico, jogue pro buraco mágico da porta 3000 dentro do container isolado'.

## Analogias e Intuições

- O CLI do Docker é o controle remoto do porto. Você aperta o botão para o guindaste pegar a caixa (run), parar ela no ar (stop), ou abrir para inspecionar os logs (logs).

## Erros Comuns

- Remover container sem olhar logs.
- Confundir imagem parada com container parado.
- Executar comandos interativos como solução permanente.

## Checklist de Entendimento

Antes de seguir, a pessoa deve conseguir responder:

- O que este conceito resolve?
- Que problema aparece quando ele é ignorado?
- Como ele se conecta com as aulas anteriores?
- Quando ele deve ser usado com cuidado?
- Qual é o próximo conceito natural depois dele?

## Prática Recomendada

- [Exercício 07 — Operando o CLI](exercicios/07-docker-cli.md)
- Explique o que faz o comando de depuração `docker exec -it <id> /bin/sh` na vida de um engenheiro procurando um bug em produção.

## Prompt Para Estudar Com IA

```text
Estou estudando CLI do Docker em uma trilha de desenvolvimento fullstack JavaScript/TypeScript.

Meu objetivo é entender o conceito, praticar e revisar sem receber respostas prontas cedo demais.

Contexto da aula:
- Descrição da aula: A aula mapeia o ecossistema local do desenvolvedor com comandos chaves do Motor Docker para rodar e depurar a nuvem no laptop.
- Descrição do modulo: O módulo introduz a cultura DevOps (CI/CD) e encerra o ciclo manual usando automação extrema, Docker e Orquestração (Kubernetes).
- Itens já estudados: Cloud Computing, Infraestrutura como Código (Terraform), Arquitetura Assíncrona.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, dê uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
