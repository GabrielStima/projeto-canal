# Exercício 12 — Pods e Deployments

## Tarefa

Escreva em texto puro a sintaxe bruta/estrutura visual de um YAML nativo de K8s. Deve ser do tipo abstrato "Deployment", requerendo o escopo `replicas: 2` de uma imagem hipotética `minha_api:latest`. Você deve demonstrar o uso correto dos blocos `spec`, e do `selector/matchLabels`. Não precisa estar indentado certinho, mas logicamente coeso.

## Corrija Sua Atividade Com IA

```text
Cenário: Estruturação Básica do Orquestrador (Pod Scaling Pattern).

Tarefa: Meu YAML (Pseudo ou original):
[COLE SUA RESPOSTA AQUI]

Critérios de correção:
1. O manifesto amarrou as Labels de template sob um "matchLabels" perfeitamente funcional para que a abstração de ReplicaSet rastreie as pods geradas?
2. Há definição de Containers/Images no `spec.template.spec` com clareza arquitetural correta da API de v1/apps do Google?
```
