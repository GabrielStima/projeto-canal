# OpenAPI na Prática

## Objetivo

Ao final desta aula, você deve ser capaz de:

- estruturar uma especificação OpenAPI pequena;
- descrever paths, operations, parameters, request bodies, responses e schemas;
- reutilizar componentes;
- entender como OpenAPI ajuda documentação, contrato e governança.

## Onde Esta Aula Entra na Formação

- Módulo: 11. API Design Profissional
- Aula anterior: Contratos de API
- Próxima aula: Backward Compatibility
- Pré-requisitos: OpenAPI Specs em nível introdutório, contratos de API, REST, JSON e status codes.

## Ideia Central

OpenAPI é uma forma padronizada de descrever APIs HTTP. Na prática, ela transforma decisões de contrato em um documento que pessoas e ferramentas conseguem ler.

Uma especificação OpenAPI costuma declarar metadados da API, servidores, paths, operações, parâmetros, request bodies, responses, schemas, componentes reutilizáveis, segurança e tags. O valor não está no arquivo YAML em si, mas no alinhamento que ele cria entre backend, frontend, QA, produto e documentação.

Para a `petcare-api`, OpenAPI permite que o time do portal do tutor e o time do painel administrativo discutam o mesmo contrato antes de escrever código.

## Tópicos da Aula

- Objeto raiz: `openapi`, `info`, `servers` e `paths`.
- Operações por método HTTP.
- `parameters` em path e query.
- `requestBody` para entrada.
- `responses` para sucesso e erro.
- `schemas` para formatos de dados.
- `components` para reutilização.
- `tags`, exemplos e documentação.
- Limites de OpenAPI.

## Exemplo Prático

```yaml
openapi: 3.1.1
info:
  title: PetCare API
  version: 1.0.0
  description: API da plataforma PetCare OS para clínicas veterinárias e tutores.
servers:
  - url: http://localhost:3000
paths:
  /pets/{petId}:
    get:
      tags:
        - Pets
      summary: Busca um pet por ID
      parameters:
        - name: petId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Pet encontrado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pet"
        "404":
          description: Pet não encontrado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
components:
  schemas:
    Pet:
      type: object
      required:
        - id
        - name
        - species
        - tutorId
      properties:
        id:
          type: string
        name:
          type: string
        species:
          type: string
          enum:
            - dog
            - cat
            - other
        tutorId:
          type: string
    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: array
          items:
            type: object
```

## Analogias e Intuições

- OpenAPI é a planta do contrato, não o prédio funcionando.
- `components` é uma biblioteca de peças reutilizáveis da especificação.
- Uma spec bem escrita é como uma receita padronizada: qualquer cozinheiro consegue seguir e o resultado fica previsível.

## Erros Comuns

- Criar a spec depois que tudo já foi decidido sem discutir contrato.
- Documentar apenas `200`.
- Não reutilizar schemas e responses comuns.
- Escrever exemplos que não batem com o backend.
- Achar que OpenAPI garante bom design automaticamente.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- O que fica no objeto raiz da especificação?
- O que uma operation descreve?
- Quando usar `parameters` e quando usar `requestBody`?
- Para que servem `schemas` e `components`?
- Como a spec pode ajudar testes e governança?

## Prática Recomendada

- Escolha dois endpoints da `petcare-api`.
- Escreva uma especificação OpenAPI mínima.
- Inclua uma resposta de sucesso e uma de erro.
- Crie pelo menos um schema reutilizável.
- Compare a spec com o comportamento real esperado.
- Continue a API existente no [Exercício 01 — Contratos da PetCare API](exercicios/01-contratos-da-petcare-api.md).

## Prompt Para Estudar Com IA

```text
Estou estudando OpenAPI na prática em uma trilha fullstack JavaScript/TypeScript.

Meu objetivo é transformar contratos de API em uma especificação legível por pessoas e ferramentas.

Contexto da aula:
- Descrição da aula: A aula aprofunda OpenAPI com paths, operations, parameters, requestBody, responses, schemas, components e tags.
- Descrição do módulo: O módulo ensina API Design Profissional com foco em contrato, evolução e governança.
- Itens já estudados: Contratos de API, REST, JSON APIs, status codes e OpenAPI introdutório.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, dê uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
