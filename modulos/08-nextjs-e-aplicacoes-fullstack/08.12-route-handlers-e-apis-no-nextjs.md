# Route Handlers e APIs no Next.js

## Objetivo

Ao final desta aula, você deve ser capaz de:

- explicar o que e um Route Handler;
- criar um endpoint simples no App Router;
- retornar JSON a partir de uma rota;
- entender o limite entre API interna e backend completo.

## Onde Esta Aula Entra no Curso

- Módulo: 08. Next.js e Aplicações Fullstack no Frontend
- Aula anterior: Chamadas de API e busca de dados
- Próxima aula: Formulários em Next.js
- Pré-requisitos: HTTP, métodos HTTP, JSON, rotas, `fetch`, TypeScript é tratamento de erros.

## Ideia Central

Route Handlers permitem criar endpoints dentro da estrutura do App Router. Eles são úteis para casos de backend-for-frontend: adaptar dados, esconder detalhes de uma API externa, receber formulários simples ou criar pequenas rotas de suporte para a interface.

Eles não eliminam a necessidade de aprender backend. O módulo de Node.js vai aprofundar APIs, arquitetura, banco de dados, segurança e operação.

## Tópicos da Aula

- Arquivo `route.ts`.
- Métodos HTTP.
- Retorno de JSON.
- Status codes.
- API interna para o frontend.
- Limites de escopo.

## Exemplo Prático

```tsx
export async function GET() {
  return Response.json([
    { id: "1", titulo: "Estudar Server Components" },
    { id: "2", titulo: "Praticar formularios" },
  ]);
}
```

Crie um endpoint `/api/estudos` para alimentar uma página do painel.

## Analogias e Intuições

- Route Handler é uma pequena janela de backend dentro do projeto Next.js.
- Backend-for-frontend adapta o servidor para as necessidades da interface.

## Erros Comuns

- Colocar regra de negócio grande demais em Route Handler.
- Ignorar status HTTP.
- Expor segredo ou token para o cliente.
- Confundir API interna com arquitetura backend completa.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- Para que serve `route.ts`?
- Qual a diferença entre `page.tsx` e `route.ts`?
- Quando retornar JSON?
- Por que status HTTP importa?
- Quando mover lógica para um backend dedicado?

## Prática Recomendada

- Crie um Route Handler `GET`.
- Consuma esse endpoint em uma página.
- Adicione retorno de erro para um caso simulado.

## Prompt Para Estudar Com IA

```text
Estou estudando Route Handlers e APIs no Next.js.

Meu objetivo é criar endpoints simples no App Router sem confundir isso com backend completo.

Contexto da aula:
- Descrição da aula: A aula mostra `route.ts`, métodos HTTP, JSON, status codes e backend-for-frontend.
- Descrição do módulo: O módulo ensina Next.js com rotas, renderização, dados, formulários, APIs internas e deploy.
- Itens já estudados: HTTP, JSON, rotas, fetch, TypeScript é tratamento de erros.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, de uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
