# Chamadas de API e busca de dados

## Objetivo

Ao final desta aula, você deve ser capaz de:

- buscar dados em uma aplicação Next.js;
- decidir se a busca acontece no servidor ou no cliente;
- entender cache e revalidação em nível introdutório;
- representar estados de dados na interface.

## Onde Esta Aula Entra na Formação

- Módulo: 08. Next.js e Aplicações Fullstack no Frontend
- Aula anterior: Suspense no contexto do Next.js
- Próxima aula: Route Handlers e APIs no Next.js
- Pré-requisitos: HTTP, `fetch`, promises, Server Components, Client Components, loading e erro.

## Ideia Central

Em Next.js, buscar dados não significa automaticamente usar `useEffect`. Muitas páginas podem buscar dados no servidor, perto da rota que precisa deles.

O Portal do Tutor pode carregar a lista de serviços disponíveis da clínica no próprio Server Component, reduzindo idas e vindas entre browser e servidor. Já a busca por nome de serviço, que responde a cada tecla digitada, pode acontecer no cliente.

Ainda assim, existem casos em que buscar no cliente faz sentido: interação instantânea, dados que dependem de estado local ou atualizações após a página carregar.

## Tópicos da Aula

- `fetch` no servidor.
- `fetch` no cliente.
- Dados públicos e dados por usuário.
- Cache e revalidação em alto nível.
- Loading, erro e vazio.
- Organização de funções de dados.

## Exemplo Prático

```tsx
async function carregarServicos() {
  const resposta = await fetch("https://api-exemplo.local/servicos");

  if (!resposta.ok) {
    throw new Error("Falha ao carregar serviços");
  }

  return resposta.json();
}
```

Use essa função em uma página de servidor que exibe o catálogo de serviços da clínica e trate a falha com o que você aprendeu na aula de error boundaries.

## Analogias e Intuições

- Buscar dados perto da página reduz viagens desnecessárias entre cliente e servidor, como pedir a lista de serviços diretamente na recepção em vez de ligar para outro andar a cada cliente.
- Cache é um acordo: ganhar velocidade aceitando uma regra clara sobre atualização.

## Erros Comuns

- Usar `useEffect` para toda busca de dados.
- Ignorar `resposta.ok`.
- Não pensar se o dado é público, privado, estático ou dinâmico.
- Misturar chamada de API com código visual sem separação mínima.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- Quando buscar dados no servidor?
- Quando buscar dados no cliente?
- Por que checar `resposta.ok`?
- Como loading e erro aparecem nessa decisão?
- Como cache pode ajudar ou atrapalhar?

## Prática Recomendada

- Crie uma função `carregarServicos`.
- Use-a em uma página Server Component que lista o catálogo da clínica.
- Simule sucesso, erro e lista vazia.

## Prompt Para Estudar Com IA

```text
Estou estudando chamadas de API e busca de dados em Next.js.

Meu objetivo é decidir quando buscar dados no servidor ou no cliente e como tratar estados de UI.

Contexto da aula:
- Descrição da aula: A aula mostra `fetch`, cache em alto nível, dados públicos ou privados e tratamento de falhas.
- Descrição do módulo: O módulo ensina Next.js com App Router, renderização, dados, APIs, formulários e deploy.
- Itens já estudados: HTTP, promises, Server Components, Client Components, loading, erro e Suspense.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, dê uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
