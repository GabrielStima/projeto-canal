# Exercício 04 — Protegendo Invariantes

## Tarefa

Escreva uma classe `EstoqueVacina`. Torne o valor `quantidade` privado (`private` em TS).
Crie os métodos `adicionar(qtd)` e `remover(qtd)` garantindo que o estoque nunca fique negativo (Invariante).

## Corrija Sua Atividade Com IA

```text
Cenário: Modelagem de Invariantes em POO.

Tarefa: Veja minha classe abaixo:
[COLE AQUI O CÓDIGO]


[COLE SUA RESPOSTA AQUI]
Critérios de correção:
1. É possível contornar a regra do estoque negativo acessando a variável diretamente?
2. O método de remoção emite um aviso claro ou throw se a invariante for ameaçada?
```
