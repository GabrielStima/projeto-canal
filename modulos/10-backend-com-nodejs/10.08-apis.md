# APIs

## Objetivo

Ao final desta aula, você deve ser capaz de:

- explicar o que é uma API;
- diferenciar API, endpoint, rota e contrato;
- entender consumidor e provedor de uma API;
- preparar o estudo de JSON APIs, REST, SOAP e GraphQL.

## Onde Esta Aula Entra no Curso

- Módulo: 10. Backend com Node.js
- Aula anterior: Frameworks Backend
- Próxima aula: JSON APIs
- Pré-requisitos: HTTP, chamadas de API no frontend, servidor HTTP e frameworks backend em alto nível.

## Ideia Central

API é uma forma definida de uma parte do sistema conversar com outra. No contexto web, normalmente pensamos em APIs HTTP: um cliente envia uma requisição para um servidor, e o servidor devolve uma resposta seguindo algum contrato.

O ponto mais importante é contrato. Uma API não é apenas código no backend. Ela define entradas, saidas, erros, formatos, regras e expectativas para quem consome.

## Tópicos da Aula

- O que é API.
- Consumidor e provedor.
- Endpoint, rota e recurso.
- Contrato de entrada e saída.
- API publica, privada e interna.
- Compatibilidade entre cliente e servidor.

## Exemplo Prático

```text
GET /aulas/10.01

Resposta esperada:
- status: 200
- corpo: dados da aula
- erro possível: aula não encontrada
```

Mesmo sem mostrar implementacao, já existe uma promessa entre cliente e servidor.

## Analogias e Intuições

- API é um cardápio: mostra o que pode ser pedido, como pedir e o que esperar.
- Contrato ruim faz o cliente adivinhar comportamento.

## Erros Comuns

- Chamar qualquer rota solta de API bem desenhada.
- Mudar resposta sem pensar em quem consome.
- Não documentar erros.
- Misturar detalhes internos com contrato publico.
- Criar endpoints pensando só no banco.

## Checklist de Entendimento

Antes de seguir, você deve conseguir responder:

- O que uma API define?
- Quem consome uma API?
- O que é endpoint?
- Por que contrato importa?
- O que pode quebrar um cliente?

## Prática Recomendada

- Escolha uma tela de listagem.
- Descreva qual API essa tela precisaria consumir.
- Especifique metodo, caminho, entrada, saída e erros.
- Escreva o que aconteceria se a resposta mudasse sem aviso.

## Prompt Para Estudar Com IA

```text
Estou estudando APIs em uma trilha fullstack JavaScript/TypeScript.

Meu objetivo é entender API como contrato entre sistemas antes de estudar JSON APIs e REST.

Contexto da aula:
- Descrição da aula: A aula apresenta API, endpoint, contrato, consumidor, provedor e compatibilidade.
- Descrição do módulo: O módulo ensina backend com Node.js, formatos de API, banco de dados e fundamentos de serviços.
- Itens já estudados: HTTP, chamadas de API, Node.js, servidor HTTP e frameworks backend.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, dê uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
