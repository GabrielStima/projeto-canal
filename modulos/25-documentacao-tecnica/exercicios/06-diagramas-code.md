# Exercício 06 — O Código Renderizado

## Tarefa

Abra o Editor (Se precisar). Aprenda a sintaxe primária Mágica do Diagramas Como Código do `Mermaid.js` suportado no Markdown do GitHub. Gere (Digite as Strings) um Diagrama de Fluxo Direcional (`graph TD` ou `sequenceDiagram`) que mostra o Cliente `A` fazendo Requisição para a API `B`, e a API gravando Assincronamente numa Fila RabbitMQ `C` que aciona um Trabalhador `D`.

## Corrija Sua Atividade Com IA

```text
Cenário: Docs-as-Code. Renderização em Markdown Nativo Github de PlantUML/MermaidJS.

Tarefa: Segue meu bloco de código Mermaid que desenha a Orquestração Assíncrona do Petcare:
[COLE AQUI O SEU CÓDIGO DO MERMAID]

Critérios de correção:
1. A sintaxe de texto do Mermaid (`graph TD; A-->B;` ou similar) está válida e pronta para o compilador do GitHub gerar as caixinhas azuis SVG na tela?
2. Representei corretamente as direções das setas com sentido no Back-End / Queue / Worker Worker/BD?
```
