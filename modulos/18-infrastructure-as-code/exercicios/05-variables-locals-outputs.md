# Exercício 05 — Variables, Locals e Outputs

## Tarefa

Declare uma variável chamada `ambiente` (`variable "ambiente" {}`). Configure a variável para ter validação restritiva em HCL (`validation { condition = ... }`), aceitando apenas as strings "dev", "stg" e "prod". Nenhuma outra deve passar no Terraform Plan.

## Corrija Sua Atividade Com IA

```text
Cenário: Parametrização Segura de Códigos Reutilizáveis.

Tarefa: Segue o código da minha Validation:
[COLE SUA RESPOSTA AQUI]

Critérios de correção:
1. A função `contains()` e o array base estão escritos na sintaxe pura de HCL v1.2+?
2. O bloco `error_message` está preenchido corretamente?
```
