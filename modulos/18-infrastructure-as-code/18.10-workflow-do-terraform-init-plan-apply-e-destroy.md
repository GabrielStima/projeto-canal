# Workflow do Terraform: Init, Plan, Apply e Destroy

## Objetivo

Ao final desta aula, você deve ser capaz de:

- explicar o workflow básico do Terraform;
- entender o papel de `init`, `plan`, `apply` e `destroy`;
- revisar um plano antes de aplicar;
- reconhecer comandos perigosos.

## Onde Esta Aula Entra na Formação

- Módulo: 18. Infrastructure as Code
- Aula anterior: Variables, Local Values e Outputs
- Próxima aula: State no Terraform
- Pré-requisitos: HCL, providers, resources, data sources, variables, outputs e leitura de plano.

## Ideia Central

O workflow básico do Terraform começa com `init`, que prepara o diretório de trabalho, configura backend e instala providers e módulos necessários. Depois, `plan` mostra a proposta de mudança. `apply` executa a mudança aprovada. `destroy` remove recursos gerenciados.

A parte mais importante do workflow não é decorar comandos. É entender que o plano precisa ser lido antes de aplicar, especialmente quando existem alterações, substituições ou destruições.

## Tópicos da Aula

- `terraform init`.
- `terraform plan`.
- `terraform apply`.
- `terraform destroy`.
- Leitura de ações no plano.
- Diferença entre planejar e executar.

## Exemplo Prático

A Rotina do Arquiteto do Hospital:

1. `terraform init`: O Dev entra no projeto vazio. O comando baixa os plugins (Traduções pra AWS/Github).
2. `terraform plan`: Ele vê a tela: `+ Vai adicionar 1 Bucket de Fotos. - Vai deletar o Banco de Sangue`. Ele percebe o erro antes de clicar em Confirmar.
3. `terraform apply`: Ele concorda. A AWS recebe o comando, cria o Bucket e cospe o IP.
4. `terraform destroy`: O projeto foi cancelado. Ele destroi as 50 peças em 1 minuto sem clicar em nada na AWS.

## Analogias e Intuições

- `Init` é comprar a panela. `Plan` é mostrar a receita pro Chef pra ver se vai ficar bom. `Apply` é colocar no forno e assar. `Destroy` é jogar a comida no lixo e lavar a panela.

## Erros Comuns

- Rodar `apply` sem ler o plano.
- Rodar `destroy` no ambiente errado.
- Ignorar substituições de recursos.
- Não perceber que `init` também lida com backend e providers.
- Usar o mesmo diretório para ambientes diferentes sem estratégia.

## Checklist de Entendimento

- O projeto já foi inicializado?
- O plano cria, altera, substitui ou destrói recursos?
- Estou no ambiente certo?
- Tenho permissão e contexto para aplicar?
- Existe backup ou estratégia de recuperação?

## Prática Recomendada

- Por que a etapa de `Plan` é considerada o 'Santo Graal' da segurança da automação em equipes corporativas?

## Prompt Para Estudar Com IA

```text
Estou estudando Workflow Init, Plan, Apply e Destroy em uma trilha de desenvolvimento fullstack JavaScript/TypeScript.

Meu objetivo é entender o conceito, praticar e revisar sem receber respostas prontas cedo demais.

Contexto da aula:
- Descrição da aula: A aula formaliza o quarteto mágico de comandos CLI que ditam toda e qualquer operação dentro do mundo Hashicorp.
- Descrição do modulo: O módulo aborda a modelagem em código de infraestruturas (AWS, Azure) ensinando do State do Terraform ao workflow declarativo de DevOps.
- Itens já estudados: Cloud Computing, VPC, IAM, Filas e Bancos Gerenciados.

Crie uma sessão de estudo com:
1. uma explicação curta do conceito;
2. 5 perguntas progressivas;
3. 3 exercícios práticos;
4. 1 desafio um pouco mais difícil;
5. exemplos de aplicação em projetos reais;
6. um checklist para eu avaliar se entendi.

Regras:
- Não entregue respostas completas antes de eu tentar responder.
- Corrija minhas respostas com explicações.
- Se eu errar, dê uma dica antes de revelar a solução.
- Ao final, sugira o próximo assunto que devo estudar.
```
